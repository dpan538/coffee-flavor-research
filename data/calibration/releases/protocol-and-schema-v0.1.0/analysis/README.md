# Analysis status

The preregistered baseline is not run on fixture rows. With zero real
observations, every empirical output is `NOT_ESTIMABLE`. The mechanical
abstention receipt is `db/data/round3d/generated/baseline_analysis.json`.
