# Data payload status

No real observation payload is present. Empty capture templates are published
at `data/calibration/templates/`; deterministic planned matrix and visibly
labelled engineering fixtures are outside reported study results.
