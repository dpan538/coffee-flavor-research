# Membership promotion decisions

All 18 inherited memberships have one current decision: 1
`PROMOTE_SOURCE_LOCAL` and 17 `RETAIN_CANDIDATE`. Rejected, cross-source and
returned-unresolved counts are zero.

The promoted `membership.roast-spice-smoke.smoke` row has a matching review,
located supporting claim, one admitted independent origin, support count 9 and
document/sample count 9. `UNSUPPORTED_PROMOTION_COUNT=0` and
`AUTOMATIC_PROMOTION_PATH_COUNT=0`.
