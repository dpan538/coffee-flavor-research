# C1 user projection validation

`SEVEN_LEVEL_MINIMUM_PASS=true`

`MEDIUM_LIGHT_PRESENT=true`

`MEDIUM_DARK_PRESENT=true`

The current view selects only categories attached to the one active project
target scheme. Historical V0 categories and observation statuses cannot appear
as concurrent current choices. Dense ordinal validation checks positions 1–7.

Protected lexical rules preserve `reported_unresolved` for filter, espresso,
omni, Nordic, City/City+, Full City, Vienna, French, and Italian roast labels.
Mapping any of these to a darkness category requires a matching approval row;
negative tests prove the unapproved write fails.
