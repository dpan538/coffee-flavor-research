# Round 3F executive synthesis

Round 3F establishes a permanent relationship and constraint architecture
without changing the canonical ontology, training a model, collecting people,
estimating question information gain or modifying the product frontend.

The implementation registers seven relationship domains and 34 important
types, inventories 17,512 existing/added relation instances, defines seven
candidate association-range anchors, admits 18 evidence-bounded memberships
and 18 logical question-range targets, and registers 35 major constraints. All
18 Round 3F negative operations fail at their named gate.

`TEXT_FIRST_LEXICAL_CANDIDATE_IS_INTENTIONAL=true`

All 107 existing `corpus.lexical_mapping_candidate` rows remain text-first and
outside the current sensory range model because their scope is preparation
language. No `OTHER` range, concept FK, exclusive membership or numeric
membership score was added.

The 18 memberships use exact existing governed question options/regions as
evidence. Eight membership rows participate in four explicit overlaps:
`caramel`, `honey`, `citrus` and `juicy`. These overlaps are research
organization only; they establish no synonym, hierarchy, neighbour, distance,
probability or transitive association.

`CANONICAL_CONCEPT_COUNT_BEFORE=130`

`CANONICAL_CONCEPT_COUNT_AFTER=130`

`ACTIVE_SENSORY_ATTRIBUTE_COUNT_BEFORE=92`

`ACTIVE_SENSORY_ATTRIBUTE_COUNT_AFTER=92`

Seven ranges remain `CANDIDATE`. No range is source-local supported,
cross-source supported or active for calibration. No association statistic was
computed in this round.
