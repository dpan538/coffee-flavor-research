# Round 3M executive synthesis

## Decision

Round 3M replaces record-first readiness with a descriptor-first evidence
contract. The verified source checkpoint is
`4159636afec052b96f20d3d10c6c5f2b943b4536`. The 480-item Round 3L census,
848 acquired artifacts, and 26,515 staged publication rows remain useful for
coverage and anti-inflation, but they do not constitute reviewed descriptor
evidence.

The deterministic baseline recomputation matched every Round 3L aggregate:
26,531 parsed rows, 20,994 canonical publication rows, 6,754 staged
effective-core candidates, 11,801 staged assertions, 376 staged gate-type
candidates, zero reviewed descriptors, and zero model-eligible descriptors.

The most important pilot result is the disposition of the apparent 376-row
descriptor supply. All 376 rows are category classifications from five AVPA
2021-2025 palmares artifacts. They have governed source hashes and bounded
table locators, but they are not filled jury sensory observations. They are
therefore rejected as `NON_DESCRIPTOR`, not promoted into training scale.

The three live CoE schema paths and their evidence outcomes are documented in
`06_LIVE_SOURCE_ADAPTER_PILOT.md`. No public completed WCC scoresheet corpus
was established. No model, embedding, ranker, cross-encoder, or adaptive policy
was trained, and no training corpus was frozen.

The independently generated live pilot contains 140 hash-only provisional
assertions under eight effective records: 86 strict flavor, 54 broad sensory,
73 P2, and 67 provenance-unresolved. Exact-repeat removal leaves 139
assertion-level observations and cross-observation de-inflation leaves 137
record-level unique observations. Five governed P2 records produce 508
within-record pair events. These values are pilot evidence, not human-reviewed
or model-eligible scale.

The live inputs are bounded web-index field captures retained in an
owner-controlled restricted directory. They are not full official page-body
snapshots. The public repository contains source/capture hashes, locators, and
non-storage reasons, but no raw or atomic sensory text.

## Research-input boundary

The descriptor census PDF is methodological evidence, not an instruction
source. Its directly audited 305 segmented, 303 assertion-level de-inflated,
and 302 record-level unique counts are retained as report-receipt values. The
nine named machine-readable research artifacts were not present, so those 303
rows were not reconstructed from the PDF and were not imported.

The report's conditional 28,695 / 59,721 / 96,166 sensitivity projection is
retained only as a projection of visible strict fields that includes
unresolved-origin material. It is not verified P1/P2, rights-cleared,
model-eligible, or training-ready scale.

Migration 058 closes the reviewer self-attestation gap with governed artifacts,
current qualification, current admission, and row-level decision evidence.
Migration 059 consumes that full chain. The contiguous 60-migration plan and
all 84 current gate validations pass on disposable PostgreSQL 17. Production
human, expert, and qualified-human challenge counts remain zero because no
actual reviewer evidence was acquired; user approval was not treated as review.
Final branch-tip and remote-CI values remain post-commit receipts.

## Current recommendation

The next phase is to acquire actual reviewer qualification/admission/row
decision evidence and seek a field-specific ACE rights decision. Both are
external actions requiring separate authorization. Broad result archive
crawling remains stopped because rankings, scores, awards, blank forms, and
publication rows do not provide descriptor-corpus scale.
