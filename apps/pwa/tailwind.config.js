/** Tailwind v3 — structural only for now; the visual design pass will define the theme. */
export default {
  content: ["./index.html", "./src/**/*.{vue,ts}"],
  theme: { extend: {} },
  plugins: [],
};
